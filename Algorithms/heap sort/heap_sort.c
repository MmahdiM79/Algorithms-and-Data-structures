#include <stdio.h>







#define swap(x, y) \
        {\
            int hold = x;\
            x = y;\
            y = hold;\
        }

#define RIGHT(x) (2*x + 2)
#define LEFT(x) (2*x + 1)







int main()
{
    return 0;
}